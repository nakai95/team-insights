# ==============================================================================
# GitHub OAuth Configuration (Required)
# ==============================================================================
# Create a GitHub OAuth App at: https://github.com/settings/developers
# For development, use callback URL: http://localhost:3000/api/auth/callback/github

# GitHub OAuth Client ID
AUTH_GITHUB_ID=your_github_oauth_client_id

# GitHub OAuth Client Secret
AUTH_GITHUB_SECRET=your_github_oauth_client_secret

# Session Encryption Secret (min 32 characters)
# Generate with: openssl rand -base64 32
AUTH_SECRET=your_generated_32_character_secret

# ==============================================================================
# Optional Configuration
# ==============================================================================

# Base URL for OAuth callbacks (auto-detected in development)
# Only needed if you have custom domain or port
# NEXTAUTH_URL=http://localhost:3000

# Debug logging (default: false)
# DEBUG=false

# Google AdSense Publisher ID (for production only)
# Format: ca-pub-XXXXXXXXXXXXXXXX
# NEXT_PUBLIC_ADSENSE_ID=ca-pub-XXXXXXXXXXXXXXXX

# ==============================================================================
# Notes
# ==============================================================================
# 1. Copy this file to .env.local and fill in your actual values
# 2. Never commit .env.local to version control
# 3. Use different OAuth apps and secrets for dev/staging/production
# 4. Rotate AUTH_SECRET periodically for security
# 5. Required OAuth scopes: read:user, user:email, repo
