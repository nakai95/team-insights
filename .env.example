# ==============================================================================
# GitHub OAuth Configuration (Required for OAuth mode)
# ==============================================================================
# Create a GitHub OAuth App at: https://github.com/settings/developers
# For development, use callback URL: http://localhost:3000/api/auth/callback/github

# GitHub OAuth Client ID
AUTH_GITHUB_ID=your_github_oauth_client_id

# GitHub OAuth Client Secret
AUTH_GITHUB_SECRET=your_github_oauth_client_secret

# Session Encryption Secret (min 32 characters)
# Generate with: openssl rand -base64 32
AUTH_SECRET=your_generated_32_character_secret

# ==============================================================================
# Alternative: GitHub Personal Access Token (Development Only)
# ==============================================================================
# ⚠️  SECURITY WARNING: Only use in development! Automatically disabled in production.
#
# For quick local setup without OAuth configuration:
# 1. Generate token at: https://github.com/settings/tokens
# 2. Select "Generate new token (classic)"
# 3. Required scopes: read:user, user:email, repo
# 4. Copy the token and set it below
# 5. When GITHUB_TOKEN is set, OAuth credentials above become optional
#
# IMPORTANT:
# - Never commit this token to version control
# - Never deploy with GITHUB_TOKEN set
# - Token provides full access to your GitHub account
# - OAuth is more secure for production use
#
# GITHUB_TOKEN=ghp_your_personal_access_token_here

# ==============================================================================
# Optional Configuration
# ==============================================================================

# Base URL for OAuth callbacks (auto-detected in development)
# Only needed if you have custom domain or port
# NEXTAUTH_URL=http://localhost:3000

# Debug logging (default: false)
# DEBUG=false

# Google AdSense Publisher ID (for production only)
# Format: ca-pub-XXXXXXXXXXXXXXXX
# NEXT_PUBLIC_ADSENSE_ID=ca-pub-XXXXXXXXXXXXXXXX

# Google Search Console Site Verification
# Get from: https://search.google.com/search-console
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=your_verification_code

# ==============================================================================
# Notes
# ==============================================================================
# 1. Copy this file to .env.local and fill in your actual values
# 2. Never commit .env.local to version control
# 3. Choose ONE authentication method:
#    - OAuth Mode: Set AUTH_GITHUB_ID, AUTH_GITHUB_SECRET, AUTH_SECRET
#    - Token Mode (dev-only): Set GITHUB_TOKEN (OAuth credentials optional)
# 4. Use different OAuth apps and secrets for dev/staging/production
# 5. Rotate AUTH_SECRET and GITHUB_TOKEN periodically for security
# 6. Required GitHub scopes: read:user, user:email, repo
